import 'package:flutter/material.dart';

class Sqlite_DB_Screen extends StatelessWidget {
  const Sqlite_DB_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Student Details"),
        centerTitle: true,
      ),
      body: Column(),
    );
  }
}
